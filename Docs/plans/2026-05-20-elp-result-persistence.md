# ELP Result 持久化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Result file persistence to Execute-LandingPrompt so every execution saves its Handoff Markdown to `<pst_root>/Result/`.

**Architecture:** Insert Step 9.5 into ELP's Phase A flow, update Handoff Template, update Phase B scaffold, and add Result/ to PST's scan ignore list.

**Tech Stack:** Markdown skill files (no code — pure prompt engineering)

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt\SKILL.md` | Modify | Add Step 9.5, update Handoff Template |
| `C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt\phase_b.md` | Modify | Add Result/ to Scaffold Rules directory list |
| `C:\Users\chenshajie\.kiro\skills\project-state-tracker\SKILL.md` | Modify | Add Result/** to §10 IGNORE list |

---

### Task 1: Add Step 9.5 to SKILL.md Execution Flow

**Files:**
- Modify: `C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt\SKILL.md`

- [ ] **Step 1: Insert Step 9.5 into Phase A flow**

In the `### Phase A — Execute (Steps 1–9)` section, after step 9, add:

```markdown
9.5. **Result 持久化** — Write the complete Handoff Markdown to `<pst_root>/Result/<artifact-id>-<YYYYMMDD-HHmmss>.md`.
     - Create `<pst_root>/Result/` if it does not exist.
     - Content = everything from `## 当前 Prompt 执行结果` through `## 给下一个 Prompt 的交接` (inclusive).
     - Timestamp uses local time, format `YYYYMMDD-HHmmss` (no colons — Windows-safe).
     - Write failure → append `⚠️ Result 写入失败: <error>` to Handoff footer. Do NOT block Phase B or alter Phase A status.
```

The section title changes from "Steps 1–9" to "Steps 1–9.5".

- [ ] **Step 2: Verify the insertion point is correct**

Confirm the new step sits between:
- Step 9: "Produce the handoff section (Markdown)."
- Phase B header: "### Phase B — PST 回流 (Steps 10–13)"

---

### Task 2: Update Handoff Template in SKILL.md

**Files:**
- Modify: `C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt\SKILL.md`

- [ ] **Step 1: Add Result 文件 line to Handoff Template**

In the `## Handoff Template` section, inside the `## 当前 Prompt 执行结果` block, after `- 验证结果:`, add:

```markdown
- Result 文件: <pst_root>/Result/<artifact-id>-<YYYYMMDD-HHmmss>.md
```

The updated template block becomes:

```markdown
## 当前 Prompt 执行结果

- 当前文件:
- 执行状态: completed / partial / blocked
- 依赖门结果: passed / forced / verify-only-override / cancelled
- 已完成:
- 未完成:
- 修改文件:
- 验证结果:
- Result 文件: <pst_root>/Result/<artifact-id>-<YYYYMMDD-HHmmss>.md
```

---

### Task 3: Update Phase B Scaffold Rules

**Files:**
- Modify: `C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt\phase_b.md`

- [ ] **Step 1: Add Result/ to scaffold directory tree**

In the `### Scaffold Rules` section, the directory tree currently shows:

```
<pst_root>/
├── status/
│   ├── status.yaml
│   └── .cache/
├── prompts/
│   ├── landing/
│   └── test/
└── (views/ and tools/ are NOT created — PST INIT handles those)
```

Change it to:

```
<pst_root>/
├── status/
│   ├── status.yaml
│   └── .cache/
├── prompts/
│   ├── landing/
│   └── test/
├── Result/
└── (views/ and tools/ are NOT created — PST INIT handles those)
```

---

### Task 4: Update PST scan_changes IGNORE list

**Files:**
- Modify: `C:\Users\chenshajie\.kiro\skills\project-state-tracker\SKILL.md`

- [ ] **Step 1: Add Result/** to §10 IGNORE list**

In the `## §10 scan_changes Path Scope (declaration)` section, the IGNORE list currently reads:

```markdown
`scan_changes.py` MUST IGNORE (never report as dirty):
- `status/**` (state + cache + lock files)
- `views/**` (regenerated by render_status.py)
- `AGENTS.md`, `README.md`, `prompts/landing/README.md`, `prompts/test/README.md` (regenerated)
- `tools/**` (skill-managed)
- `outputs/**` (LP execution artifacts — never source)
- Any file whose `path` in `artifacts[]` starts with `external:` (agent-managed, not on disk)
```

Add after `outputs/**`:

```markdown
- `Result/**` (ELP execution result archive — never source)
```

- [ ] **Step 2: Commit all changes**

```bash
cd C:\Users\chenshajie\.kiro\skills\Execute-LandingPrompt
git add SKILL.md phase_b.md
git commit -m "feat: add Result persistence (Step 9.5) — save Handoff Markdown to pst_root/Result/"

cd C:\Users\chenshajie\.kiro\skills\project-state-tracker
git add SKILL.md
git commit -m "feat: add Result/** to scan_changes IGNORE list"
```
